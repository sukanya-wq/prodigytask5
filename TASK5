TASK5:

pip install diffusers transformers torch pillow --quiet
pip install transformers 
pip install tensorflow
pip install nltk
pip install spacy
pip install BLIP transformers

from diffusers import StableDiffusionImg2ImgPipeline
from PIL import Image
import torch
#starting the program

device = "cuda" if torch.cuda.is_available() else "cpu"

pipe = StableDiffusionImg2ImgPipeline.from_pretrained(
    "runwayml/stable-diffusion-v1-5"
).to(device)

# Upload your content image in Colab first
init_image = Image.open("content.jpg").convert("RGB").resize((512,512))

prompt = "Make this image in Van Gogh painting style"

image = pipe(prompt=prompt, image=init_image, strength=0.75).images[0]

image

plt.plot()
plt.axis(off)


#this will generate the output 
